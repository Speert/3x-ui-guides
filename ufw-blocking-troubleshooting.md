# Инструкция: Проверка и решение блокировки вашей панели 3X-UI и VPN
 
## Симптомы блокировки UFW
 
- ✅ Сервер отвечает на ping
- ✅ SSH работает ЕСЛИ соединение было установлено ДО блокировки (ESTABLISHED соединения не разрываются)
- ✅ НОВОЕ SSH соединение НЕ устанавливается (блокируются новые SYN пакеты)
- ✅ VPN subscription не обновляется (новые HTTPS handshakes блокируются)
- ✅ VPN панель (3X-UI) недоступна через браузер (новые TCP соединения блокируются)
- ✅ С других IP всё работает: мобильный интернет, другой VPN
- ✅ Через 5-10 минут всё само восстанавливается

---

## ⚡ БЫСТРОЕ РЕШЕНИЕ

**Если не хотите читать всю инструкцию** - добавьте ваш IP в исключения firewall:

1. **Узнайте свой IP:** https://ifconfig.me или `curl -4 ifconfig.me`
2. **Добавьте IP в исключения на сервере** (замените YOUR_IP):
```bash
sudo ufw insert 1 allow from YOUR_IP
sudo ufw reload
```
3. **Проверяйте работу VPN**

---
 
## ⚠️ Рекомендация: Настройка MTU в клиенте

**Установите MTU = 1400 в настройках VPN клиента** (iOS/Android/Windows/macOS). Это предотвращает фрагментацию пакетов и блокировки firewall. **Не используйте MTU 9000!**

## Диагностика и решение
 
### 1. Проверьте блокируется ли ваш IP
 
НА ВАШЕМ КОМПЬЮТЕРЕ (MacBook/Windows/Linux) выполните:
```bash
# Узнайте свой IP
curl -4 ifconfig.me
```
 
Или откройте в браузере: https://ifconfig.me
 
НА СЕРВЕРЕ выполните (замените YOUR_IP на IP из предыдущей команды):
```bash
# Проверьте логи UFW
sudo grep "YOUR_IP" /var/log/ufw.log | tail -20
```

**Пример блокировки в логе:**
```
Dec 28 09:15:23 server kernel: [UFW BLOCK] IN=eth0 OUT= MAC=... SRC=203.0.113.45 DST=... LEN=60 TOS=0x00 PREC=0x00 TTL=54 ID=12345 PROTO=TCP SPT=54321 DPT=443 SEQ=... ACK=... WINDOW=65535 RES=0x00 SYN URGP=0
Dec 28 09:15:24 server kernel: [UFW BLOCK] IN=eth0 OUT= MAC=... SRC=203.0.113.45 DST=... LEN=60 TOS=0x00 PREC=0x00 TTL=54 ID=12346 PROTO=TCP SPT=54322 DPT=1025 SEQ=... ACK=... WINDOW=65535 RES=0x00 SYN URGP=0
Dec 28 09:15:25 server kernel: [UFW BLOCK] IN=eth0 OUT= MAC=... SRC=203.0.113.45 DST=... LEN=60 TOS=0x00 PREC=0x00 TTL=54 ID=12347 PROTO=TCP SPT=54323 DPT=443 SEQ=... ACK=... WINDOW=65535 RES=0x00 SYN URGP=0
```

Обратите внимание:
- `[UFW BLOCK]` - маркер блокировки
- `SRC=203.0.113.45` - ваш IP блокируется
- `DPT=443` или `DPT=1025` - порты VPN панели и подключений
- `SYN` - попытка установить новое соединение
 
### 2. Если видите блокировки - добавьте whitelist
 
НА СЕРВЕРЕ выполните:
```bash
# Добавьте ваш IP в whitelist (замените YOUR_IP)
sudo ufw insert 1 allow from YOUR_IP
 
# Перезагрузите UFW
sudo ufw reload
 
# Проверьте правила
sudo ufw status numbered
```
 
### 3. Проверьте работу
- Обновите подписку в VPN клиенте несколько раз
- Подключитесь к VPN
 
## Почему UFW блокирует IP?
 
### Причина: Rate Limiting (ограничение частоты)
В UFW есть встроенная защита от флуда в /etc/ufw/before.rules:
```
-A ufw-not-local -m limit --limit 3/min --limit-burst 10
```
 
Параметры:
- --limit 3/min - максимум 3 пакета в минуту
- --limit-burst 10 - разрешено до 10 пакетов подряд, потом блокировка
 
Когда срабатывает:
1. Множественные обновления подписки - каждое обновление = несколько TCP пакетов (SYN, ACK, RST)
2. Частые переподключения к VPN - каждое подключение = новый handshake
3. Обновление нескольких устройств одновременно с одного IP
4. Диагностика (traceroute, mtr) - генерирует много пакетов
5. **Высокий MTU (9000) вызывает фрагментацию → больше пакетов → чаще блокировки**
 
Что блокируется:
- RST пакеты (закрытие соединений)
- SYN пакеты (новые соединения)
- Не блокируются ESTABLISHED соединения (SSH продолжает работать)
 
Время блокировки: обычно 5-10 минут, пока счётчик не сбросится
 
## Откат (если нужно)
 
НА СЕРВЕРЕ:
```bash
# Удалите whitelist правило
sudo ufw status numbered
sudo ufw delete 1  # номер правила с вашим IP
sudo ufw reload
```
